import styled, { keyframes } from 'styled-components';

// 채널등록페이지스타일
export const ChannelupDiv=styled.div`
    background-color: #1A1A1A;
    height: 1080px;
    color: white;
`

export const ChannelupTitle=styled.div`

`

export const ChannelupContent=styled.div`

`

export const ChannelupInput=styled.input`

`

// 채널보기페이지스타일